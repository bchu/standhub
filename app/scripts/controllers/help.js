'use strict';

angular.module('standhubApp')
  .controller('HelpCtrl', ['$scope', 'firebase','angularFire', function ($scope, angularFire) {
    
    $scope.submitRequest = function(tags,comment) {

    };

  }]);